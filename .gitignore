node_modules
.next
tsconfig.tsbuildinfo
.vercel

# Secrets — never commit. .env.example is the only tracked env file.
.env*
!.env.example

# Eval harness: run output and the API-call cache are machine-local.
# Results that matter live in Supabase (history) and are summarised in git.
eval/runs/
eval/.cache/

# Golden-set working state. questions.jsonl (the reviewed set) IS tracked — it
# is the source of truth every metric is computed against. These two are not:
# candidates are regenerable from the cache, and .reviewed is local progress.
eval/golden/questions.candidate.jsonl
eval/golden/.reviewed

# Local agent/editor config
.claude/
CLAUDE.md
.cursor/
.idea/
.vscode/
*.pem
*.key
