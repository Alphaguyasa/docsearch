/**
 * Golden set generation — PHASE 1 STUB.
 *
 * Exists so `npm run eval:golden` resolves to a real file and fails with a clear
 * message instead of a module-not-found error. Phase 0 explicitly defers golden
 * set generation.
 */
console.error(
  "not implemented — golden set generation lands in Phase 1 " +
    "(see docs/EVAL_HARNESS.md).",
);
process.exit(1);
